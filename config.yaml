---

inventory:
    plugin: SimpleInventory
    options:
        host_file: "host.yaml"
        group_file: "groups.yaml"
        defaults_file: "defaults.yaml"

runner:
    plugin: threaded
    option:
        num_workers: 10